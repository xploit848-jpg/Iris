<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/iris_bg">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="Memory"
        android:textColor="@color/iris_text_primary"
        android:textSize="20sp" />

    <ListView
        android:id="@+id/memory_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@color/iris_surface_alt"
        android:dividerHeight="1dp" />

    <TextView
        android:id="@+id/clear_memory_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:gravity="center"
        android:text="Clear All Memory"
        android:textColor="@color/orb_speaking"
        android:clickable="true"
        android:focusable="true" />

</LinearLayout>
